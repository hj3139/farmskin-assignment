import styled from 'styled-components';

export const HeaderWrapper = styled.div`
  position: fixed;
  width: 100%;
  height: 80px;
  top: 0;
  background-color: #eaf3fe;
  z-index: 2;

  #header-title {
    width: auto;
    margin: 24px 440px;
    font-size: 22px;
    font-weight: 700;
    line-height: 32px;
  }
`;
